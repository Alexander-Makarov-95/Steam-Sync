// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:D:/automatehdmi/db/gamesListDatabase.sqlite"
}

model games {
  game_id    Int    @id 
  name  String
  image_url  String
}
